language: python
python:
  - "2.7"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  PYTHONPATH=$(pwd) coverage run --source paystack paystack/test_customer.py
after_success:
  coveralls


# coverage run --source paystack -m unittest discover